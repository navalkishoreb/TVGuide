<resources>

  <!-- Base application theme. -->
  <style name="AppTheme"
      parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="LoadingText">
    <item name="android:layout_height">wrap_content</item>
    <item name="android:layout_width">wrap_content</item>
    <item name="android:layout_centerHorizontal">true</item>
    <item name="android:maxLines">@integer/loading_view_text_max_lines</item>
    <item name="android:ellipsize">end</item>
    <item name="android:paddingRight">@dimen/loading_view_side_margin</item>
    <item name="android:paddingLeft">@dimen/loading_view_side_margin</item>
  </style>

  <style name="LoadingLayout">
    <item name="android:layout_height">match_parent</item>
    <item name="android:layout_width">match_parent</item>
    <item name="android:gravity">center_horizontal</item>
    <item name="android:layout_marginTop">@dimen/loading_view_top_margin</item>
    <item name="android:layout_marginLeft">@dimen/loading_view_side_margin</item>
    <item name="android:layout_marginRight">@dimen/loading_view_side_margin</item>
  </style>

  <style name="LoadingProgressBar"
      parent="Widget.AppCompat.ProgressBar.Horizontal">
    <item name="android:layout_height">wrap_content</item>
    <item name="android:layout_width">match_parent</item>
    <item name="android:indeterminate">true</item>
  </style>

</resources>
